{
  "name": "hubspot-central-brain",
  "version": "0.1.0",
  "private": true,
  "description": "HubSpot Central Brain — hub-and-spoke system for developer advocacy",
  "scripts": {
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "lint:fix": "eslint 'src/**/*.{ts,tsx}' --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "validate": "npm run lint && npm run typecheck && npm run test",
    "provision": "tsx src/scripts/provision-objects.ts",
    "build": "esbuild src/app/functions/LinearWebhook.ts src/app/functions/SyncToLinear.ts src/app/functions/AppSettingsApi.ts --bundle --platform=node --target=node18 --outdir=src/app/functions",
    "dev": "hs project dev",
    "upload": "npm run build && hs project upload",
    "upload:dev": "npm run build && hs project upload --profile=dev",
    "upload:staging": "npm run build && hs project upload --profile=staging",
    "upload:prod": "npm run build && hs project upload --profile=prod"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@hubspot/cli": "^8.0.0",
    "@types/node": "^18.0.0",
    "esbuild": "^0.28.2",
    "eslint": "^9.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@hubspot/api-client": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
